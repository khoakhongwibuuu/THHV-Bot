services:
  chv-bot:
    build: .
    env_file:
      - .env
    volumes:
      - ./logs:/usr/src/bot/logs
      - ./configs:/usr/src/bot/configs
      - ./modules:/usr/src/bot/modules
    restart: unless-stopped
    environment:
      - TOKEN=${TOKEN}
      - CLIENT_ID=${CLIENT_ID}
      - OWNER_ID=${OWNER_ID}
    networks:
      - multi-domain

networks:
  multi-domain:
    external: true
    name: multi-domain
