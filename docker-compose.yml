services:
  chv-bot:
    container_name: codeforce-thhv-bot
    build: .
    volumes:
      - ./logs:/usr/src/bot/logs
      - ./config:/usr/src/bot/config
    deploy:
      restart_policy:
        condition: on-failure
        delay: 5s
        max_attempts: 3
        window: 120s